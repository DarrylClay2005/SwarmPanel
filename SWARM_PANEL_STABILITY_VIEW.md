# SwarmPanel Stability Endpoint

Added `GET /api/stability` for active Aria degraded cooldowns, recent repair journal entries, and existing metrics snapshot. Backend-first so it can be tested safely before adding more UI complexity.
